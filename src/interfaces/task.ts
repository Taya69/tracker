

export interface Task {
    _id?: string;
    name: string;
    description: string,
    // dateOfCreate?: Date;
    // dateDeadline? : Date,
    // fileSrc?: string,
    // order?: Number,
    priority?: string,
    imgSrc? : string
        
  }